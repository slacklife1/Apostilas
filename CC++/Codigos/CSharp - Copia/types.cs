public  class object
{

  // Constructors
  public .ctor();

  // Methods
  public virtual bool Equals(object obj);
  private void FieldGetter(System.Reflection.FieldInfo fldInfo, Object& val);
  private void FieldSetter(System.Reflection.FieldInfo fldInfo, object val);
  protected virtual void Finalize();
  private Type GetExistingClass();
  public virtual int GetHashCode();
  public Type GetType();
  private Type InternalGetClass();
  protected object MemberwiseClone();
  public virtual string ToString();
} //
int  i = 10;

Console.WriteLine (i.ToString ());

return 0;
int  i;

string cszNumberString = "100";

i = int.Parse(cszNumberString);

Console.WriteLine (i.ToString ());

 